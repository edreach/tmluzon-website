rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // WARNING: These rules are completely open for development.
    // All reads and writes are allowed to your storage bucket.
    // You should secure your storage before deploying to production.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
