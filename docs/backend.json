{
  "entities": {
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product listed on the TMLUZON website.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the product."
        },
        "name": {
          "type": "string",
          "description": "The name of the product."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the product, potentially augmented by AI."
        },
        "specifications": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "value": { "type": "string" }
            }
          }
        },
        "imageUrls": {
          "type": "array",
          "description": "URLs of the high-resolution images showcasing the product.",
          "items": {
            "type": "string",
            "format": "uri"
          }
        },
        "price": {
          "type": "number",
          "description": "The price of the product."
        },
        "showPrice": {
          "type": "boolean",
          "description": "Whether to display the price on the public site."
        },
        "type": {
          "type": "string",
          "description": "The type of the product (e.g., Wall Mounted, Floor Mounted)."
        },
        "brand": {
          "type": "string",
          "description": "The brand of the product."
        },
        "subType": {
          "type": "string",
          "description": "The sub-type of the product (e.g., Indoor, Outdoor)."
        },
        "discontinued": {
          "type": "boolean",
          "description": "Whether the product is discontinued and should not be displayed on the public site."
        },
        "stockStatus": {
          "type": "string",
          "description": "The stock status of the product.",
          "enum": [
            "In Stock",
            "Out of Stock",
            "Made to Order"
          ]
        }
      },
      "required": [
        "name",
        "description",
        "price",
        "type",
        "brand",
        "subType"
      ]
    },
    "Service": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Service",
      "type": "object",
      "description": "Represents a service offered by TMLUZON.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the service."
        },
        "name": {
          "type": "string",
          "description": "The name of the service."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the service."
        },
        "imageUrls": {
          "type": "array",
          "description": "URLs of the images showcasing the service.",
          "items": {
            "type": "string",
            "format": "uri"
          }
        },
        "price": {
          "type": "number",
          "description": "The price of the service."
        },
        "showPrice": {
          "type": "boolean",
          "description": "Whether to display the price on the public site."
        }
      },
      "required": [
        "name",
        "description",
        "price"
      ]
    },
    "CustomerReview": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CustomerReview",
      "type": "object",
      "description": "Represents a customer review for a product.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the customer review."
        },
        "productId": {
          "type": "string",
          "description": "Reference to Product. (Relationship: Product 1:N CustomerReview)"
        },
        "userId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N CustomerReview)"
        },
        "rating": {
          "type": "number",
          "description": "The rating given by the customer (e.g., 1-5 stars)."
        },
        "comment": {
          "type": "string",
          "description": "The customer's comments about the product."
        },
        "date": {
          "type": "string",
          "description": "The date the review was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "productId",
        "userId",
        "rating",
        "comment",
        "date"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user of the application.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Unique identifier for the user, from Firebase Auth."
        },
        "name": {
          "type": "string",
          "description": "The name of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        }
      },
      "required": [
        "uid",
        "name",
        "email"
      ]
    },
    "SiteSettings": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SiteSettings",
      "type": "object",
      "description": "Stores site-wide settings for the TMLUZON application.",
      "properties": {
        "logoUrl": {
          "type": "string",
          "description": "URL of the site logo.",
          "format": "uri"
        },
        "aboutUsContent": {
          "type": "object",
          "description": "Structured content for the About Us page.",
          "properties": {
            "intro_p1": { "type": "string" },
            "intro_p2": { "type": "string" },
            "intro_p3": { "type": "string" },
            "overview_p1": { "type": "string" },
            "service_consultation": { "type": "string" },
            "service_supply": { "type": "string" },
            "service_supply_list": { "type": "string" },
            "service_installation": { "type": "string" },
            "service_maintenance": { "type": "string" },
            "statement_purpose": { "type": "string" },
            "statement_vision": { "type": "string" },
            "statement_mission": { "type": "string" }
          }
        }
      },
      "required": []
    },
    "Pricelist": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Pricelist",
      "type": "object",
      "description": "Represents a pricelist file for a specific brand.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the pricelist entry."
        },
        "brand": {
          "type": "string",
          "description": "The brand associated with the pricelist."
        },
        "title": {
          "type": "string",
          "description": "The display title for the pricelist file."
        },
        "fileName": {
          "type": "string",
          "description": "The name of the uploaded PDF file."
        },
        "fileUrl": {
          "type": "string",
          "description": "The download URL for the pricelist file from Firebase Storage.",
          "format": "uri"
        }
      },
      "required": [
        "brand",
        "title",
        "fileName",
        "fileUrl"
      ]
    },
    "Brand": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Brand",
      "type": "object",
      "description": "Represents a brand partner, featured on the About Us page.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "logoUrl": { "type": "string", "format": "uri" },
        "websiteUrl": { "type": "string", "format": "uri" },
        "imageHint": { "type": "string" },
        "sortOrder": { "type": "number", "description": "A number to control the display order on the public site. Lower numbers appear first." }
      },
      "required": ["name", "logoUrl"]
    },
    "NewsArticle": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "NewsArticle",
      "type": "object",
      "description": "Represents a news article or update.",
      "properties": {
        "title": { "type": "string" },
        "date": { "type": "string", "format": "date" },
        "imageUrl": { "type": "string", "format": "uri" },
        "imageHint": { "type": "string" },
        "content": { "type": "string" }
      },
      "required": ["title", "date", "content"]
    },
    "Inquiry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Inquiry",
      "type": "object",
      "description": "Represents a customer inquiry or order from the public website.",
      "properties": {
        "customerName": { "type": "string" },
        "customerEmail": { "type": "string" },
        "shippingAddress": {
          "type": "object",
          "properties": {
            "address": { "type": "string" },
            "city": { "type": "string" },
            "state": { "type": "string" },
            "zip": { "type": "string" }
          },
          "required": ["address", "city", "state", "zip"]
        },
        "inquiryDate": { "type": "string", "format": "date-time" },
        "totalAmount": { "type": "number" },
        "status": {
          "type": "string",
          "enum": ["New", "Viewed", "Completed"]
        },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "productId": { "type": "string" },
              "productName": { "type": "string" },
              "quantity": { "type": "number" },
              "price": { "type": "number" }
            },
            "required": ["productId", "productName", "quantity", "price"]
          }
        }
      },
      "required": [
        "customerName",
        "customerEmail",
        "shippingAddress",
        "inquiryDate",
        "totalAmount",
        "status",
        "items"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Stores product information. Only admins can create, update, or delete products.",
          "params": [
            {
              "name": "productId",
              "description": "The unique identifier for the product."
            }
          ]
        }
      },
      {
        "path": "/services/{serviceId}",
        "definition": {
          "entityName": "Service",
          "schema": {
            "$ref": "#/backend/entities/Service"
          },
          "description": "Stores services offered.",
          "params": [
            {
              "name": "serviceId",
              "description": "The unique identifier for the service."
            }
          ]
        }
      },
      {
        "path": "/news/{newsId}",
        "definition": {
          "entityName": "NewsArticle",
          "schema": {
            "$ref": "#/backend/entities/NewsArticle"
          },
          "description": "Stores news articles for the public site. Admins can manage these.",
          "params": [
            {
              "name": "newsId",
              "description": "The unique identifier for the news article."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/customer_reviews/{customerReviewId}",
        "definition": {
          "entityName": "CustomerReview",
          "schema": {
            "$ref": "#/backend/entities/CustomerReview"
          },
          "description": "Stores customer reviews for products. Each review is associated with a customer via path-based ownership.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the customer who wrote the review."
            },
            {
              "name": "customerReviewId",
              "description": "The unique identifier for the customer review."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Documents in this collection indicate admin privileges. Existence determines access to admin-only functions.",
          "params": [
            {
              "name": "userId",
              "description": "The user's unique ID."
            }
          ]
        }
      },
      {
        "path": "/admin/dashboard/settings/tmluzon",
        "definition": {
          "entityName": "SiteSettings",
          "schema": {
            "$ref": "#/backend/entities/SiteSettings"
          },
          "description": "This document will contain settings for the TMLUZON application.",
          "params": []
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores public user profile information.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/pricelists/{pricelistId}",
        "definition": {
          "entityName": "Pricelist",
          "schema": {
            "$ref": "#/backend/entities/Pricelist"
          },
          "description": "Stores downloadable pricelist files. Admins can create, update, or delete these.",
          "params": [
            {
              "name": "pricelistId",
              "description": "The unique identifier for the pricelist."
            }
          ]
        }
      },
      {
        "path": "/brands/{brandId}",
        "definition": {
          "entityName": "Brand",
          "schema": {
            "$ref": "#/backend/entities/Brand"
          },
          "description": "Stores brand partners displayed on the website. Admins can manage these.",
          "params": [
            {
              "name": "brandId",
              "description": "The unique identifier for the brand."
            }
          ]
        }
      },
      {
        "path": "/inquiries/{inquiryId}",
        "definition": {
          "entityName": "Inquiry",
          "schema": {
            "$ref": "#/backend/entities/Inquiry"
          },
          "description": "Stores customer inquiries from the public website. Admins can view and manage these.",
          "params": [
            {
              "name": "inquiryId",
              "description": "The unique ID for the inquiry."
            }
          ]
        }
      }
    ],
    "reasoning": "Based on the requirements, the data structure prioritizes a flat structure and path-based ownership for user-generated content (customer reviews, cart items, and orders) to ensure Authorization Independence. Product data is stored in a top-level collection accessible to all users but manageable only by admins.\\n\\nThe structure promotes Authorization Independence by avoiding `get()` calls in security rules. Customer reviews, cart items, and orders are stored under the `/users/{userId}` path, inherently associating them with the user. This allows direct enforcement of ownership rules based on the request's `auth.uid` without needing to fetch parent documents. For the admin dashboard, a single document is used to manage the application's settings. Since only admins should have access, a document `/roles_admin/{uid}` will determine read/write privileges.\\n\\nThe structure supports QAPs by:\\n\\n1.  **Products:** Secure `list` operations on products are possible because they are stored in a top-level collection, and admin privileges are checked via a separate `/roles_admin/{uid}` document.\\n2.  **User-Specific Data:** `list` operations on customer reviews, cart items, and orders are secured by the path-based ownership `/users/{userId}`, ensuring users can only access their own data.\\n3.  **Settings:** Only admin users have access to the settings."
  }
}
